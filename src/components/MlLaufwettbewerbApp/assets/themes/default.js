import { yellow, blueGrey } from "@material-ui/core/colors";

const colorTheme = {
  palette: {
    primary: blueGrey,
    secondary: yellow,
    chart: {
      gridColor: "#ebebeb",
    },
  },
};

export default colorTheme;
