import { yellow, lightBlue } from "@material-ui/core/colors";

const colorTheme = {
  palette: {
    primary: lightBlue,
    secondary: yellow,
    chart: {
      gridColor: "#ebebeb",
    },
  },
};

export default colorTheme;
