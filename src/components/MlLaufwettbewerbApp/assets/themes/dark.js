import { yellow, lime, cyan } from "@material-ui/core/colors";

const colorTheme = {
  palette: {
    type: "dark",
    primary: cyan,
    secondary: lime,
    info: yellow,
    background: {
      default: "#222222",
    },
    chart: {
      gridColor: "#444",
    },
  },
};

export default colorTheme;
