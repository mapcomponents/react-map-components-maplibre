import { lime, cyan } from "@material-ui/core/colors";

const colorTheme = {
  palette: {
    type: "dark",
    primary: cyan,
    secondary: lime,
    background: {
      default: "#222222",
    },
    chart: {
      gridColor: "#444",
    },
  },
};

export default colorTheme;
