import { Checkbox, ListItemIcon, styled } from '@mui/material';

export const CheckboxListItemIcon = styled(ListItemIcon)({
	minWidth: '30px',
});

export const CheckboxStyled = styled(Checkbox)({
	padding: 0,
});
